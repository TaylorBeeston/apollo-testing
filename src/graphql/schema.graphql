type Greeting {
    greeting: String!
}

type User {
    id: String!
    name: String!
}

type Query {
    myResolver(name: String!): Greeting!
    user(id: String!): User
}

type Mutation {
    createUser(name: String!): User
}
